# Godot
test-project/.godot/
test-project/.import/
test-project/export_presets.cfg.bak

# Engine source clones (huge, regenerable)
rsanchezsaez-godot/
OpenXR-OSX/

# Xcode build artifacts
out/xcode-visionos/build/
out/xcode-visionos/*.xcodeproj/xcuserdata/
out/xcode-visionos/*.xcodeproj/project.xcworkspace/xcuserdata/

# Built PCKs (regenerable from test-project/)
out/xcode-visionos/GodotVisionPilot.pck

# Built engine + camera xcframeworks (~950 MB; contain the BORROWED libgodot.a and its .bak copies).
# NEVER commit these to this public repo — regenerate from the engine fork. See CLAUDE.md "Do NOT".
out/xcode-visionos/*.xcframework/

# Templates / large binary bundles
templates.tpz
Godot.app/

# Captures (KB has them; pilot only keeps source-of-truth)
# Keep cascade-2026-05-27.mp4 since it's the canonical first capture
captures/frame_*.jpg

# macOS noise
.DS_Store
clancey-godot/

# Local Claude agent/session state (memory, worktrees) — never commit to a public repo
.claude/

# Compiled sim-input tap (build from tools/SimInputTap.swift)
tools/siminputtap

# compiled SimHands canned MC sender (build from the .swift)
/tools/simhands_canned_sender
